# Generated by `deno task extension` - do not edit manually.
# Installs the pg_semantic_platform extension into PostgreSQL's extension directory.
#   make install   (uses pg_config; may need sudo)

EXTENSION = pg_semantic_platform
DATA = $(wildcard pg_semantic_platform--*.sql)

PG_CONFIG ?= pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)

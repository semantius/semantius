# Generated by `deno task extension` - do not edit manually.
# Installs the pg_semantius extension into PostgreSQL's extension directory.
#   make install   (uses pg_config; may need sudo)

EXTENSION = pg_semantius
DATA = $(wildcard pg_semantius--*.sql)

PG_CONFIG ?= pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)

# Generated by `deno task extension` - do not edit manually.
# Installs the semantius extension into PostgreSQL's extension directory.
#   make install   (uses pg_config; may need sudo)

EXTENSION = semantius
DATA = $(wildcard semantius--*.sql)

PG_CONFIG ?= pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)

# dependencies
/node_modules

# build output
/dist

# env files — these hold only PUBLIC VITE_ values, but keep them out of git anyway
.env
.env.local
.env*.local

# typescript / vite
*.tsbuildinfo

# misc
.DS_Store

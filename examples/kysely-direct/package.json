{
  "name": "@semantius/example-kysely-direct",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "List users from Semantius core with Kysely, authenticating via an OAuth token over SASL OAUTHBEARER (PostgreSQL 18).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/kysely-direct.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "kysely": "^0.27.4"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
